[project]
name = "sentinelapi-worker"
version = "0.1.0"
description = "Background worker for SentinelAPI"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.9"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

