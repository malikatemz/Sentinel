[project]
name = "sentinelapi"
version = "0.1.0"
description = "Python SDK for SentinelAPI"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

